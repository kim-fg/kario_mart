using UnityEngine;

namespace KarioMart.Menu.Toggles
{
    public class ToggleGamemodeItem : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
